Generate a single valid Python module (pytest) that runs the scenarios below against an HTTP API.

Scenarios (JSON array of objects with keys test_id, endpoint, method, query, payload, expected_status, covered_condition_ids):
SCENARIOS_PLACEHOLDER

Base URL string to embed in code:
BASE_URL_PLACEHOLDER

REQUIREMENTS
1. Output ONLY Python source. No markdown. No backticks. No explanations before or after the code.
2. One module: imports (pytest, requests, typing), BASE_URL constant, one make_request function, test_scenarios list, then tests.
3. make_request signature MUST be exactly:
   def make_request(method: str, endpoint: str, params: Optional[Dict] = None, data: Optional[Dict] = None, headers: Optional[Dict] = None, expected_status: int = 200) -> requests.Response:
   Implementation: build url = BASE_URL + endpoint; call requests.get(..., params=params) for GET; requests.delete similarly; for POST/PUT/PATCH use json=data and pass params=params when non-empty; include timeout=10; on RequestException call pytest.fail(...).
4. Every test MUST call make_request with keyword argument expected_status=scenario["expected_status"] so the caller documents the intended status even though requests returns a real Response.
5. Use scenario["query"] for params and scenario["payload"] for JSON body. Treat missing keys as {}.
6. Define test_scenarios as a Python list of dicts copied from the JSON (normalize so each dict has query and payload keys).
7. Use @pytest.mark.parametrize with ids=[s["test_id"] for s in test_scenarios] so pytest node ids contain the test_id.
8. In the parametrize test function, assert response.status_code == scenario["expected_status"] with a message that includes response.text.
9. If expected_status is not 204, attempt response.json() inside try/except; on failure pytest.fail with a clear message.
10. Do not duplicate make_request or BASE_URL. Do not include placeholder comments.

OUTPUT ENVELOPE (required by the API)
Return exactly one JSON object (no markdown fences) with a single key "python_code" whose value is a JSON string containing the entire Python module. Properly escape quotes and newlines inside that string.
Example shape: {"python_code": "import pytest\\n..."}
